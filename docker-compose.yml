version: '3.8'
services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: tenex
      POSTGRES_PASSWORD: tenex
      POSTGRES_DB: tenex
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
volumes:
  postgres-data:
